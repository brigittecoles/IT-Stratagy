# IT Strategy Diagnostic — Codex MCP Configuration
# This file auto-configures the MCP server when Codex runs from this repo.

[mcp_servers.it-strategy-diagnostic]
command = "npx"
args = ["tsx", "mcp-server/src/index.ts"]
startup_timeout_sec = 15
tool_timeout_sec = 60
